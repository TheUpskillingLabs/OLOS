# OLOS — Architecture Brief (SUPERSEDED)

> **This document is superseded.** It described an early plan — a Python/FastAPI
> backend, a 13-week cycle, and a seven-phase machine — that does not match what
> shipped. OLOS is a single Next.js + Supabase app.
>
> **Read [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) instead** for how the codebase is
> actually organized, and [`SCHEMA.md`](../SCHEMA.md) for the current data model.
>
> The file is kept (rather than deleted) so existing links don't 404. Its historical
> content lives in git history if you need the original planning context.
