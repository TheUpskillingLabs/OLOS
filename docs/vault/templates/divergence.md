---
title: 
date: 
status: accepted
kind: divergence
tags: [spec-divergence]
related: []
---

# 

## What the spec says

Quote or cite the original: `TUL_MVP_Spec.md` §, a PRD in `docs/`, or a roadmap anchor.
Be specific enough that the gap is checkable.

## What the build does instead

The current behaviour, with the code path or migration that implements it.

## Why it moved

Link the decision note that caused this: `[[ADR-NNNN-...]]`. If there is no decision
note, this divergence is unexplained: write the decision first, then come back.

## Is the spec now wrong, or is the build?

One of three: the spec should be amended, the build should be brought back, or the two
are legitimately allowed to differ and the spec stays as historical intent. Say which,
and say who decides if it is open.

## Downstream effects

Other parts of the spec that assumed the original behaviour and are now also off.
