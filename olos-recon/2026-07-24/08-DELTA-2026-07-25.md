# 08 — DELTA ADDENDUM: changes since the Phase-1 pull (audited 2026-07-25)

Method: remote-ref snapshot diff (`git for-each-ref` before/after `git fetch --all --prune`) + GitHub search for issues/PRs `updated:>=2026-07-24`. This isolates the post-audit delta exactly, without re-running any lane.

## Scope of the delta

- **One new branch**: `origin/claude/triangulator-workflow-gaps-yv0dzb` at `9bfd3ad`, forked from `b1abda1` (= current main content) [FACT: ref diff; merge-base].
- **One new PR**: #302 "Proposal gallery, map links, and participant-visible survey results" — open, created 2026-07-25T11:59Z via the Claude GitHub App, driven by `brendanwhitaker` [FACT: #302].
- **Nothing else moved**: `main` still `d260022`, `dev` still `b1abda1` content, zero issues updated since 07-24, no other PR activity [FACT: ref diff; search `updated:>=2026-07-24` → issues 0, PRs 1].
- Consequence: **every Lane A–G conclusion stands unmodified**. This delta is additive: open unmerged PRs go 7 → 8.

## PR #302 contents (3 commits, +686/−58, 11 files, no schema change)

1. `686795c` (11:22Z) — Always-on proposal gallery page (`app/(dashboard)/cycles/[cycle_id]/proposals/page.tsx`) + "Link to your map" (`repo_url`) on proposal submissions.
2. `ba64860` (13:06Z) — Opens civics field-survey results to cycle participants; observations get stable `#r-<id>` anchors.
3. `9bfd3ad` (14:01Z) — Propose→vote→pods arc seam fixes + contact-info scrub (`redactContactInfo`) on the participant survey surface.

CI green (`ci` check success 14:02Z); Vercel preview deployed (project under `hq-1586s-projects` — corroborates Lane C's HQ/Vercel operator thread) [FACT: check runs; vercel bot comment].

## What it changes in the audit picture

- **A decision is being made right now, in a PR, with no reviewer** — the exact pattern Lanes B/C/G documented. #302's body changes the survey-results policy from *approved-only* to *all non-rejected (pending marked "awaiting review")* for enrolled participants, expressly because 17 in-app submissions are invisible — **"there's no approve UI yet"** — and asks "Shout if you'd rather keep the approved-only gate" [FACT: #302 body]. In a zero-review repo, that shout can only come from a co-lead. This goes to the top of the decision-needed queue and is a concrete Day-1 review target.
- **New moderation-workflow gap surfaced**: survey observations have review states but no approve UI exists [FACT: #302 body]. Visibility policy is being adjusted around a missing tool rather than the tool being built — leadership call on sequencing.
- **Triangulator cross-repo integration is growing undocumented**: `repo_url` links proposals to Triangulator working folders; survey `#r-<id>` anchors are cited by Triangulator extract cards (TheUpskillingLabs/triangles#72). Second repo (`triangles`) now depends on OLOS URL/anchor contracts; no OLOS doc describes this [FACT: #302 body; Lane F found no mention]. Adds to decision-debt §2 (undocumented architecture direction).
- **Privacy posture: consistent with the audit's concern, and mitigating** — participant-facing observation text is scrubbed (`lib/content/survey-results.ts` + unit tests), contact columns never selected, map URLs scheme-checked vs `javascript:`/`data:` injection [FACT: diffstat; #302 body]. Gallery preserves blind-voting anonymity: "same no-author-attribution convention as the ballot", selects only `id, statement_text, proposal_data, created_at` [FACT: proposals/page.tsx:24,80]. No new #117-style leak found in this PR.
- CODEOWNERS still auto-routes #302's review request to the absent co-lead — reviewing it is literally already assigned to him [INFER: high — CODEOWNERS unchanged on main; request list not re-fetched].

## Gaps

- Did not run the PR's test suite locally (CI already green; read-only audit).
- `TheUpskillingLabs/triangles#72` not read (repo outside session scope) — the Triangulator-side contract is [UNVERIFIED].
- Whether pending-visible-by-default has any consent implication for the 17 pending submitters (they submitted knowing… what?) — [UNVERIFIED; product/consent question for the team].
