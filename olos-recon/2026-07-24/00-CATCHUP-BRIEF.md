# OLOS Catch-Up Brief

*For the returning R&D co-lead. Written 2026-07-25 from a read-only audit of the repo and GitHub. Cutoff: your last commit, 2026-06-18 (`d8d4741`). Every claim traces to the lane files (`01`–`08`) and, through them, to a command you can re-run. [FACT: sha]/[FACT: #n] = verified; [INFER] = reasoned; the gaps are named at the end.*

## Where you left it, and what it is now

When you stepped away, OLOS was a ~10-week-old Next.js app with 30 migrations, roughly 50 API routes, no CI, no test runner, and a dev→main promotion ritual you personally ran [FACT: a164dff = mainline tip 06-04→06-18; 07-lane-ops §headline]. Five weeks later it is a substantially different system: **91 migrations (61 new — two-thirds of the chain postdates you)** [FACT: 04-lane-schema §1], **118 API routes (74 added)** [FACT: 05-lane-api §1], 59 database tables, and six or seven entire subsystems that did not exist in your world: a public-content CMS with Luma-synced events, the Learning/Leadership Logs instrument, a LinkedIn-style social layer (follows, posts, likes, comments), a sector/org-cycles/local-labs model, an owner lifecycle console with destructive reset RPCs, field surveys with a sensemaker flow, and a rebuilt roles system with `participant_roles` as the single authority [FACT: 04-lane-schema §1–2].

The volume came in one flood. June was nearly silent — zero PRs and zero issue closures from 06-19 to 07-04 [FACT: 02-lane-issues §2] — then July produced 592 commits and 163 PRs in three weeks [FACT: 00-ORIENTATION; 02-lane-issues §4]. The 29-day gap in dev→main promotions (06-04 → 07-03) brackets your departure exactly [FACT: 01-lane-branches §1]. Whatever else is true, the team paused, regrouped, and then sprinted.

Also new since you left: a shared org account (`TheLabsHQ`, first commit 07-03) participates in development and merges [FACT: 03-lane-people §1], and roughly **half of all agent-authored commits on dev since the cutoff (90 of 180) arrived by direct agent pushes with no PR and no attributable human driver** [FACT: 03-lane-people §1]. Issue #214 was filed and self-closed in seven seconds purely to confess that the largest July wave shipped "with no tracking issue" [FACT: #214].

## The four changes that actually matter

**1. Production was not migrated — it was wiped and rebuilt.** On 2026-07-11 the team reset prod (`DROP SCHEMA public CASCADE`) and re-provisioned it from a generated `baseline.sql` at chain position 00077, because a rehearsal proved the migration chain no longer replays cleanly (it failed at 00054; the files had drifted from the history that built dev) [FACT: 04-lane-schema §6, from `docs/prod-migration-plan.md` on unmerged branch `claude/dev-prod-migration-plan-5pumez`, commit 8a967dd]. Three consequences you must internalize: (a) prod's migration ledger no longer mirrors the repo chain, and whether the 14 post-baseline migrations (00078–00091) have been hand-applied is **recorded nowhere** [UNVERIFIED — prod DB off-limits to this audit]; (b) two prod-only security fixes made during the cutover (enabling RLS on `participant_erasures`; tightening `participants_insert`) were **never codified as migrations**, so dev, local, and any fresh rebuild regress to the unfixed state [FACT: execution record item 6; 00058:74 has no ENABLE RLS]; (c) the only record of this entire event sits on an unmerged branch — mainline documentation (`docs/environments.md`, last touched 07-09) still describes the world from two days *before* the reset [FACT: 06-lane-docs §Ghost].

**2. Review disappeared as a concept — and it structurally points at you.** There are **zero formal reviews on all 244 PRs ever**; the 163 post-departure PRs merged with a median of 3.6 minutes, ~97% self-merged; neither `dev` nor `main` has branch protection; and one PR merged after seven consecutive red CI runs on 07-13 [FACT: 02-lane-issues §4; 03-lane-people §3; 07-lane-ops §2,§5]. What substitutes for review is CI (which itself only dates to 07-04), PR-body self-verification checklists, and after-the-fact audit documents. Meanwhile CODEOWNERS still lists you and auto-requests your review on every PR — the review system's only named reviewer has been absent for five weeks, and the most active merger (inferno-gh) isn't in the file at all [FACT: .github/CODEOWNERS; 03-lane-people §3]. This is not an abstraction: as of this morning there are **8 open PRs and all of them route to you**, including PR #302 (opened 07-25) which contains an explicit participant-data-visibility policy change with the author asking "Shout if you'd rather keep the approved-only gate" — a shout only you can now make [FACT: 08-DELTA].

**3. The product's measurement spine pivoted from Pulse to Learning Logs.** The pulse-check subsystem you knew — including its cron — was retired from the nav and its reminder route deleted on 07-04; Learning Logs (weekly, baseline, leadership variants, migrations 00040/00069/00087/00091) replaced it as the engagement instrument, with the moderator health model repointed accordingly (`lib/moderator/log-health.ts`) [FACT: 306ded1; 04-lane-schema §1; 06-lane-docs §Drift]. Your §3 invariant "pulse checks are the sole phase-window exception" is still technically true, but the thing it protects is now legacy. The cycles model also changed shape underneath: single-active-cycle became an invariant (00048) and was then rescoped three times, landing on "one global HQ open cycle, labs as sub-cohorts via `pods.lab_id`, org cycles per-lab" (00067) [FACT: 04-lane-schema §3e].

**4. Knowledge is fragmenting onto dead branches.** Three load-bearing artifacts exist only on never-merged branches: the prod-reset record (above); `docs/EVOLUTION.md` plus the fully reconciled roadmap tracker (branch `claude/olos-dev-pod-project-creation-uz3wez`, which also carries a parallel-universe labs model with migration numbers that collide with dev's); and `docs/audit/VIBE_SCAN_2026-07.md`, a 38-finding code-quality ledger that merged commits on main *cite as their justification* [FACT: 06-lane-docs §Ghost]. Both open p1 issues (#212, #213) reference doc paths and code symbols that 404 on mainline [FACT: #212/#213 bodies vs grep]. The good news buried here: the July "documentation/reality audit" was genuinely thorough — it just never landed.

## dev vs main — the inversion you should expect

The premise "what I missed is sitting unshipped on dev" is **false**. dev has been fully promoted; dev == main since 07-22/23 (#296/#299/#301), and promotion has been near-daily since the cutover [FACT: 01-lane-branches §1]. Everything above is **live in production alpha with real participant data**. The unshipped surface is instead: 8 open PRs (7 docs/UX-scale + today's #302) and ~11 branches with real residual content — including your own two final branches (`docs/comms-preview`, `docs-cleanup`; the latter would now delete a CLAUDE.md that root CLAUDE.md still references), an abandoned-but-live ops SQL that would remove four participants from the current cycle (`24610cf`), and the ghost-docs branch that can only be salvaged by cherry-pick because its migration numbers are long since taken [FACT: 01-lane-branches §4a,§8]. "Shipping dev" is a no-op; the real question is which of these branches to salvage, and you are the requested reviewer on essentially all of it.

## The biggest risk right now

**The schema chain, prod, and the code are three diverging sources of truth, and the divergence is unrecorded.** Concretely: prod ≠ chain (baseline rebuild + uncodified prod-only fixes), chain ≠ docs (SCHEMA.md says "19 tables"; there are 59; the consent/erasure/email-audit tables — `agreement_acceptances`, `email_log`, `participant_erasures` — appear in **no documentation anywhere**, in a live alpha holding real PII) [FACT: 04-lane-schema §2; 06-lane-docs §Freshness], and post-baseline migrations carry known-sharp edges: 00083 rewrites live profile URLs with no redirect, 00082 destroys availability selections by design, 00089 requires a manual backfill nobody recorded executing, 00090 embeds 31 survey respondents' names/emails/phones in git history [FACT: 04-lane-schema §4,§7]. Add the operational edge: every cron route authenticates against `Bearer ${CRON_SECRET}` with no unset-guard — an environment missing the var accepts the literal `Bearer undefined` — and `CRON_SECRET` is absent from `.env.local.example` [FACT: 07-lane-ops §3]. And the revocation cron you "temporarily" unscheduled on 05-31 has never been rescheduled; the rewritten two-stage warn→revoke route has zero invokers (open p1 #213) [FACT: 4361f91; vercel.json; 05-lane-api §6]. None of these is an active incident; together they are the incident waiting for the next cycle's load.

A second risk deserves one sentence: an unfiled, security-adjacent finding from #117's closure — `GET /api/pods/[pod_id]/solution-proposals` returns `participant_id` to any authed pod member during blind voting — was flagged as deserving "its own small p1 fix" on 07-11 and no issue exists [FACT: comment 4941719186].

## What the team prioritized, and what that tells you

They prioritized, in order: getting a real cohort live (production cutover, Cycle 3 launch train, registration/calendar work), replacing the measurement instrument (Learning Logs), making the product feel alive (social layer, dashboard UX, public site), and — notably — **manufacturing their own guardrails mid-sprint**: CI, vitest, the migration-number checker, issue/PR templates, CODEOWNERS, the PR-only rule, and three successive audit/reconciliation passes (07-04 gap audit, 07-05/07 tracker sweep, 07-11 reality audit) all date from the three weeks after you left [FACT: 07-lane-ops §1; 02-lane-issues §2]. Read that as a team under cohort-deadline pressure, aware it was moving too fast, repeatedly pausing to re-establish ground truth — and each time writing the reconciliation into a snapshot that immediately went stale or never merged. They did not lack discipline; they lacked a second pair of hands with authority to hold the line. The rule they wrote on 07-13 was violated within a day, including by the shared HQ account, because nothing enforces it [FACT: 01-lane-branches §3; 07-lane-ops §5].

What they conspicuously did *not* do: review each other's work, apply your migration/prod-apply discipline (prod drifted 11 migrations behind by 07-06, then got reset), restore the revocation cron, or absorb your ops-scripts surface — `migrate.py` and the bulk-invite tooling have had zero human touches since you left [FACT: 03-lane-people §2].

## What you most need to ask, because the evidence cannot answer it

The repo cannot tell you: **(1) prod's actual state** — are 00078–00091 applied, was `supabase_migrations` re-seeded post-baseline, is `CRON_SECRET`/`LUMA_API_KEY` set in Vercel, has the 00089 backfill run? Someone with Supabase/Vercel dashboard access holds this. **(2) Who drove the unattributed half of the July wave** — the 90 direct agent pushes, the #214 wave, the ghost branch — and relatedly, who operates `TheLabsHQ`. This is bus-factor mapping, not attribution for its own sake: the July architecture's context holder is currently anonymous. **(3) Intent behind three abandoned things** — the #230 revert branch (created, never PR'd; the reverted code is live), your own parked `docs-cleanup`/`comms-preview` branches (do you still want them?), and whether the revocation cron is off by policy or by drift. **(4) The pending decision in PR #302** — participants can now see unmoderated survey observations marked "awaiting review," expressly because no approve UI exists; that default was chosen this morning by one person asking for objections into a review vacuum [FACT: 08-DELTA].

Full evidence: `01-CHANGE-LEDGER.md`. People and bus factors: `02-OWNERSHIP-MAP.md`. Decisions to confirm or make: `03-DECISION-DEBT.md` — that file is your first-week agenda. Where to plug in first: `04-REENTRY-PLAN.md`. What to write or commission: `05-DOC-DEBT.md`.
